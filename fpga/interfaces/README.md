# Streaming Interfaces

This directory contains reusable streaming interfaces shared by all FPGA modules.

Current interfaces:

- axis_stream_if

The project follows an AXI4-Stream inspired architecture to keep all packet-processing modules consistent.

Future extensions:

- TKEEP
- TUSER
- Modports
- Clocking blocks
